const initialState = [{
  name: 'React',
  url: 'http://petstore.swagger.io/v2/swagger.json',
}];

/**
 * SpecList
 *
 * @param {Array}  state
 * @param {Object} action
 *
 * @return {Array}
 */
function specList(state = initialState) {
  return state;
}

export default specList;
