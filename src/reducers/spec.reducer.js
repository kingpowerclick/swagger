const initialState = [{
  name: 'React',
  url: 'https://facebook.github.io/react',
}];

/**
 * SpecList
 *
 * @param {Array}  state
 * @param {Object} action
 *
 * @return {Array}
 */
function specList(state = initialState) {
  return state;
}

export default specList;
